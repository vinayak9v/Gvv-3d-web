# Next.js
.next/
out/

# Dependencies
node_modules/

# Logs
*.log

# Environment
.env
.env.local
/src/generated/prisma

# Admin-uploaded files (images/PDFs), saved outside public/ — served via
# /api/uploads/[...path] (see src/lib/upload.ts). Not source-controlled.
/uploads/

# Local debug screenshots / scratch capture outputs (root-level only; real assets
# live under public/). Generated by the tools/*.mjs Playwright helpers.
/*.png
